"""Operational tools for the Customer Operations Agent.

Narrow, independently testable interfaces used by graph nodes: the
read-only `CustomerOperationsStore` (`customer_data.py`) and the mutating
`CustomerActionStore` (`action_store.py`).
"""
